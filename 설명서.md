# 프로젝트 설명서

## 📁 파일 구조 및 역할

### 🎯 핵심 파일들

#### 1. **tt.js** (메인 Vue 컴포넌트 - 1,786줄)
**역할**: 상품 카드 표시 및 카테고리 추천 시스템의 핵심 컴포넌트

**주요 기능**:
- 상품 정보 표시 (이미지, 상품명, 가격, 브랜드 등)
- 카테고리 추천 및 검증
- 키워드 하이라이트 시스템
- 툴팁 기반 카테고리 선택
- 다중 선택 기능

**Import 의존성**:
```javascript
import modal from "./ttmodal.js";
import { kwd } from "./kwd.js";
import { validCategories } from "./검카체크.js";
import { copycopy, copyToClipboard, toggleMoreInfo, ... } from "./sub.js";
import { PRODUCT_STATUS, CATEGORY_VALIDATION } from "./constants.js";
import { calculateTooltipPosition, generateTooltipHTML, ... } from "./tooltip.js";
import { BigImageModule } from "./bigimage.js";
```

**Data 속성**:
- `id`: 상품 번호
- `dealname`: 상품명
- `categories`: 카테고리 정보 객체 (search, display, corrected, full)
- `highlighted`: 하이라이트된 텍스트들
- `activeFilters`: 활성 키워드 필터
- `recommendedCategories`: 추천 카테고리 목록
- `tooltipStyle`: 툴팁 스타일 정보

**Computed Properties**:
- `cardColorClass()`: 카드 색상 클래스 결정
- `filteredCategoriesForDisplay()`: 필터링된 카테고리 표시
- `highlightedSearchCategoryForCategory()`: 검색 카테고리 하이라이트
- `highlightedDisplayCategoryForCategory()`: 표시 카테고리 하이라이트
- 하위 호환성을 위한 computed들 (fullcategory, display_category 등)

**주요 메서드**:

1. **키워드 처리**:
   - `extractProductKeywords(productName)`: 상품명에서 키워드 추출
   - `extractKoreanSubstrings(text)`: 한글 부분 문자열 추출
   - `findCommonWords(textWords, compareWords)`: 공통 단어 찾기

2. **카테고리 관리**:
   - `validateCategory()`: 카테고리 유효성 검증
   - `findCategoriesByMultipleKeywords(keywords)`: 다중 키워드로 카테고리 찾기
   - `applyCategoryRecommendation(category)`: 추천 카테고리 적용

3. **하이라이트 시스템**:
   - `highlightText(text, compareWords, options)`: 통합 하이라이트 함수
   - `updateHighlights()`: 모든 하이라이트 업데이트
   - `updateKeywordHighlights()`: 키워드 하이라이트 상태 업데이트

4. **툴팁 관리**:
   - `showKeywordSpecificTooltip(event, keyword)`: 키워드별 툴팁 표시
   - `positionAndShowTooltip(event, recommendations)`: 툴팁 위치 계산 및 표시
   - `hideKeywordTooltip()`: 툴팁 숨기기

5. **API 통신**:
   - `get_InnerKeyword()`: 상품 데이터 로드
   - `fetchProductData()`: API 호출
   - `populateDataFromBackendApi(productData)`: 데이터 채우기

#### 2. **sub.js** (유틸리티 함수 모음 - 158줄)
**역할**: tt.js에서 분리된 재사용 가능한 유틸리티 함수들

**Export 함수들**:
- `copycopy(event, copyToClipboard)`: 클릭된 요소 텍스트 복사
- `copyToClipboard(text)`: 클립보드 복사 (async)
- `closeCard(message, emit)`: Vue 카드 닫기
- `toggleMoreInfo(showMoreInfo)`: 더보기 토글
- `openCatalogLink(rep_ctlg_no)`: 카탈로그 링크 열기
- `getProductTypeStyle(std_prd_yn)`: 상품 타입별 CSS 스타일
- `getCategoryInputStyle(isCategoryValid)`: 카테고리 입력 스타일
- `getSubstring(a, b, c, d)`: 부분 문자열 추출
- `clearData()`: 데이터 초기화

#### 3. **constants.js** (상수 정의 - 48줄)
**역할**: 프로젝트 전체에서 사용되는 상수들 중앙 관리

**Export 상수들**:
- `PRODUCT_STATUS`: 상품 상태 (단일상품/다중상품)
- `CATEGORY_VALIDATION`: 카테고리 검증 메시지
- `COLORS`: 색상 상수
- `CATEGORY_ORDER`: 카테고리 순서
- `CATEGORY_GROUPS`: 카테고리 그룹핑

#### 4. **tooltip.js** (툴팁 관리 - 201줄)
**역할**: 카테고리 추천 툴팁 시스템

**Export 함수들**:
- `calculateTooltipPosition(event, recommendations)`: 툴팁 위치 계산
- `adjustTooltipBounds(position, tooltip, viewport, rect)`: 툴팁 경계 조정
- `createTooltipStyle({left, top, width, height})`: 툴팁 스타일 생성
- `generateTooltipHTML(...)`: 툴팁 HTML 생성
- `generateCategoryListHTML(...)`: 카테고리 목록 HTML 생성
- `removeAllTooltips()`: 모든 툴팁 제거
- `applyKeywordHighlightToCategory(...)`: 카테고리 키워드 하이라이트

#### 5. **validators.js** (검증 함수 - 60줄)
**역할**: 카테고리 및 키워드 검증 로직

**Export 함수들**:
- `isValidSearchCategory(category)`: 유효한 검색 카테고리 확인
- `isRecommendationEnabled(parent)`: 추천 기능 활성화 확인
- `filterMeaningfulDuplicates(duplicateKeywords)`: 의미있는 중복 키워드 필터링
- `categoryContainsKeyword(category, keyword)`: 카테고리 키워드 포함 여부

#### 6. **keyword.js** (키워드 처리 - 343줄)
**역할**: 키워드 추출 및 처리 전문 모듈

**KeywordProcessor 클래스 메서드들**:
- `extractProductKeywords(productName)`: 상품명 키워드 추출
- `removeExcludedContent(text)`: 괄호 내용 제거
- `extractKoreanSubstrings(text)`: 한글 부분 문자열 추출
- `findCommonWords(textWords, compareWords)`: 공통 단어 찾기
- `categoryContainsKeyword(category, keyword)`: 카테고리 키워드 포함 확인

### 🎨 UI 관련 파일들

#### 7. **ttmodal.js** (모달 컴포넌트 - 132줄)
**역할**: 로그인 모달 및 설정 모달 Vue 컴포넌트

**기능**:
- 로그인 모달 (사용자명/비밀번호)
- 설정 모달 (이미지 크기, 글씨 크기 조절)
- 미리보기 기능

**Data 속성**:
- `username`, `password`: 로그인 정보
- `imageSize`, `fontSize`: 크기 설정
- `showSettings`: 설정 모달 표시 여부

**Methods**:
- `login()`: 로그인 처리 (async)
- `updateImageSize()`, `updateFontSize()`: 크기 업데이트
- `resetToDefault()`: 기본값 초기화

#### 8. **font.js** (폰트 제어 - 354줄)
**역할**: 이미지 크기와 글씨 크기 동적 조절

**Export 함수**:
- `initializeFontSizeControl()`: 폰트 크기 제어 초기화

**내부 함수들**:
- `loadSettings()`: 설정 로드
- `saveSettings(settings)`: 설정 저장
- `applyImageSize(size)`: 이미지 크기 적용
- `applyFontSize(size)`: 폰트 크기 적용
- `createSettingsPanel()`: 설정 패널 생성

#### 9. **bigimage.js** (이미지 확대 - README 참조)
**역할**: 상품 이미지 확대 모달 기능

### 📊 데이터 파일들

#### 10. **kwd.js** (키워드 데이터 - 45줄)
**역할**: 카테고리별 키워드 매핑 데이터

**구조**:
```javascript
export const kwd = [
  {
    "검카": "화장품/미용",
    "키워드": "쉐이빙/케어/쉐이빙젤/페이스/..."
  },
  // ... 11개 카테고리
]
```

#### 11. **검카체크.js** (카테고리 검증 - 9,615줄)
**역할**: 유효한 카테고리 목록 데이터

**구조**:
```javascript
export const validCategories = [
  "도서 > 국어/외국어사전 > 국어사전",
  "도서 > 국어/외국어사전 > 한영/영한사전",
  // ... 약 9,600개 카테고리
]
```

#### 12. **cate.js** (카테고리 데이터 - 42,371줄)
**역할**: 전체 카테고리 구조 데이터 (대용량)

#### 13. **categoryRanking.js** (카테고리 순위 - 7,905줄)
**역할**: 카테고리별 순위 데이터

### 🌐 API 서버 파일들

#### 14. **api.js** (Express 서버 - 330줄)
**역할**: 11번가 상품 정보 조회 API 서버

**주요 기능**:
- 11번가 검색 API 연동
- 상품 정보 가공 및 반환
- 다중 상품 조회 지원

**API 엔드포인트**:
- `GET /api/product/:prdNo`: 단일 상품 조회
- `POST /api/products`: 다중 상품 조회

**내부 함수들**:
- `fetchProductInfo(prdNo)`: 단일 상품 정보 조회
- `fetchMultipleProducts(prdNos)`: 다중 상품 정보 조회
- `buildSearchCategoryPath(product)`: 검색 카테고리 경로 생성
- `buildDisplayCategoryPath(product)`: 표시 카테고리 경로 생성

#### 15. **11stqc.js** (QC 서버 - 65줄)
**역할**: 품질관리 데이터베이스 연동 서버

**기능**:
- MSSQL 데이터베이스 연결
- 상품 QC 정보 조회
- 배치 처리 지원

### 🎮 메인 애플리케이션

#### 16. **t.js** (메인 앱 - 763줄)
**역할**: Vue 애플리케이션 메인 인스턴스

**주요 기능**:
- 상품 검색 및 표시
- 카테고리 매핑 관리
- 다중 선택 기능
- 설정 관리

**Data 속성**:
- `messages`: 상품 목록
- `categoryMapping`: 카테고리 매핑
- `selectedProducts`: 선택된 상품들
- 각종 표시 옵션들

**Methods**:
- `addMessage()`: 상품 추가
- `removeMessage()`: 상품 제거
- `updateCategoryMapping()`: 카테고리 매핑 업데이트
- `exportToExcel()`: 엑셀 내보내기

### 🧪 테스트 파일들

#### 17. **test.js** (테스트 스크립트 - 19줄)
**역할**: API 테스트용 Node.js 스크립트

#### 18. **tett.js** (테스트 데이터 - 7,865줄)
**역할**: 대용량 테스트 데이터

### 📄 기타 파일들

#### 19. **index.html** (메인 페이지)
**역할**: 애플리케이션 진입점 HTML

#### 20. **외부 라이브러리들**
- `vue.min.js`: Vue.js 프레임워크
- `axios.min.js`: HTTP 클라이언트
- `bootstrap.min.css`, `bootstrap.bundle.min.js`: Bootstrap UI
- `jquery-3.6.0.slim.min.js`: jQuery

## 🔄 데이터 흐름

1. **상품 검색**: t.js에서 상품번호 입력
2. **API 호출**: tt.js에서 api.js로 상품 정보 요청
3. **데이터 처리**: 11번가 API에서 상품 정보 가져와서 가공
4. **키워드 추출**: 상품명에서 키워드 추출 (keyword.js)
5. **카테고리 추천**: 키워드 기반으로 카테고리 추천 (validCategories 참조)
6. **하이라이트**: 키워드 하이라이트 적용
7. **툴팁 표시**: 사용자 클릭 시 추천 카테고리 툴팁 표시
8. **카테고리 적용**: 사용자가 선택한 카테고리를 수정카테고리에 적용

## 🎯 핵심 기능 요약

1. **상품 정보 표시**: 이미지, 상품명, 가격, 브랜드, 카테고리
2. **키워드 기반 카테고리 추천**: 상품명 분석으로 적절한 카테고리 제안
3. **실시간 하이라이트**: 키워드와 카테고리 간 매칭 시각화
4. **인터랙티브 툴팁**: 클릭 기반 카테고리 선택 인터페이스
5. **다중 선택 및 일괄 처리**: 여러 상품 동시 관리
6. **엑셀 내보내기**: 작업 결과 저장
7. **설정 관리**: UI 크기 조절 등 사용자 설정