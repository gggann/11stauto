# 코드 수정 제안서

## 🚨 중복 코드 문제

### 1. **함수 중복 (심각도: 높음)**

#### A. tt.js와 keyword.js 간 중복
```javascript
// 중복 함수들:
- extractProductKeywords()
- removeExcludedContent()
- extractKoreanSubstrings()
- findCommonWords()
```

**문제점**:
- tt.js에 1,786줄 중 상당 부분이 keyword.js와 중복
- 유지보수 시 두 곳을 모두 수정해야 함
- 로직 불일치 가능성

**제안**:
```javascript
// tt.js에서 중복 함수 제거하고 import 사용
import { 
  extractProductKeywords,
  removeExcludedContent,
  extractKoreanSubstrings,
  findCommonWords
} from "./keyword.js";
```

#### B. tt.js와 validators.js 간 중복
```javascript
// 중복 함수:
- categoryContainsKeyword()
- filterMeaningfulDuplicates() (로직 유사)
```

**제안**:
```javascript
// tt.js에서 validators.js import 추가
import { 
  categoryContainsKeyword,
  filterMeaningfulDuplicates
} from "./validators.js";
```

### 2. **파일 구조 문제 (심각도: 중간)**

#### A. tt.js 파일 크기 (1,786줄)
**문제점**:
- 단일 파일이 너무 큼
- 여러 책임을 가짐 (SRP 위반)
- 디버깅 및 유지보수 어려움

**제안 구조**:
```
components/
├── ProductCard.vue (메인 컴포넌트)
├── CategoryRecommendation.js (카테고리 추천 로직)
├── KeywordHighlight.js (하이라이트 로직)
└── TooltipManager.js (툴팁 관리)
```

#### B. 데이터 파일 최적화
**문제점**:
- cate.js (42,371줄), categoryRanking.js (7,905줄) 등 대용량 파일
- 메모리 사용량 과다
- 초기 로딩 시간 증가

**제안**:
```javascript
// 지연 로딩 적용
const categoryData = () => import('./cate.js');
const rankingData = () => import('./categoryRanking.js');

// 또는 JSON 파일로 분리
import('./data/categories.json');
```

## 🔧 성능 최적화 제안

### 1. **메모이제이션 적용 (심각도: 중간)**

#### A. 키워드 추출 결과 캐싱
```javascript
// 현재: 매번 키워드 추출
extractProductKeywords(productName) {
  // 복잡한 정규식 처리...
}

// 제안: 메모이제이션 적용
const memoizedExtractKeywords = memoize(extractProductKeywords);
```

#### B. 카테고리 검색 결과 캐싱
```javascript
// 현재: 매번 전체 카테고리 검색
findCategoriesByMultipleKeywords(keywords) {
  return validCategories.filter(category => {
    // 9,600개 카테고리 전체 검색...
  });
}

// 제안: 인덱스 기반 검색
const categoryIndex = createKeywordIndex(validCategories);
```

### 2. **이벤트 리스너 최적화 (심각도: 중간)**

#### A. 중복 이벤트 리스너 제거
```javascript
// 현재: attachKeywordEvents()에서 매번 새 리스너 추가
attachKeywordEvents() {
  keywordHighlights.forEach(element => {
    if (element._clickHandler) {
      element.removeEventListener('click', element._clickHandler);
    }
    element._clickHandler = (event) => {
      this.handleKeywordClick(event, keyword);
    };
    element.addEventListener('click', element._clickHandler);
  });
}

// 제안: 이벤트 위임 사용
document.addEventListener('click', (event) => {
  if (event.target.classList.contains('keyword-highlight')) {
    const keyword = event.target.getAttribute('data-keyword');
    this.handleKeywordClick(event, keyword);
  }
});
```

### 3. **디바운싱 개선 (심각도: 낮음)**

#### A. 툴팁 표시 디바운싱
```javascript
// 현재: 120ms 고정 디바운싱
if (now - this._tooltipTick < 120) return;

// 제안: 적응형 디바운싱
const debounceTime = this.activeFilters.length > 0 ? 50 : 200;
if (now - this._tooltipTick < debounceTime) return;
```

## 🏗️ 아키텍처 개선 제안

### 1. **모듈 분리 (심각도: 높음)**

#### A. 현재 구조의 문제점
```
tt.js (1,786줄)
├── Vue 컴포넌트 로직
├── 키워드 처리 로직
├── 카테고리 추천 로직
├── 하이라이트 로직
├── 툴팁 관리 로직
├── API 통신 로직
└── 유틸리티 함수들
```

#### B. 제안하는 구조
```
core/
├── ProductCard.vue (Vue 컴포넌트만)
├── CategoryService.js (카테고리 관련 로직)
├── KeywordService.js (키워드 관련 로직)
├── HighlightService.js (하이라이트 로직)
├── TooltipService.js (툴팁 관리)
└── ApiService.js (API 통신)

utils/
├── keyword.js (키워드 유틸리티)
├── validators.js (검증 함수)
├── constants.js (상수)
└── helpers.js (기타 헬퍼)

data/
├── categories.json (카테고리 데이터)
├── keywords.json (키워드 데이터)
└── rankings.json (순위 데이터)
```

### 2. **상태 관리 개선 (심각도: 중간)**

#### A. 현재 문제점
```javascript
// tt.js에서 부모-자식 간 복잡한 데이터 전달
if (this.$parent && this.$parent.updateCategoryMapping) {
  this.$parent.updateCategoryMapping(this.message, this.corrected_category);
}
```

#### B. 제안: Vuex 또는 Pinia 도입
```javascript
// store/modules/products.js
export const productStore = {
  state: {
    products: [],
    categoryMappings: {},
    selectedProducts: new Set()
  },
  mutations: {
    UPDATE_CATEGORY_MAPPING(state, { productId, category }) {
      state.categoryMappings[productId] = category;
    }
  }
}
```

## 🐛 버그 수정 제안

### 1. **메모리 누수 방지 (심각도: 높음)**

#### A. 이벤트 리스너 정리
```javascript
// 현재: 컴포넌트 제거 시 이벤트 리스너 남아있음
// 제안: beforeDestroy에서 정리
beforeDestroy() {
  this.removeAllEventListeners();
  this.cancelCloseTimer();
  removeAllTooltips();
}
```

#### B. 타이머 정리
```javascript
// 현재: tooltipCloseTimer가 정리되지 않을 수 있음
// 제안: 명시적 정리
cancelAllTimers() {
  if (this.tooltipCloseTimer) {
    clearTimeout(this.tooltipCloseTimer);
    this.tooltipCloseTimer = null;
  }
}
```

### 2. **에러 처리 개선 (심각도: 중간)**

#### A. API 에러 처리
```javascript
// 현재: 기본적인 에러 처리만
catch (error) {
  console.error("API 호출 실패:", error);
  this.dealname = "API 호출 실패";
}

// 제안: 상세한 에러 처리
catch (error) {
  const errorHandler = new ApiErrorHandler();
  const userMessage = errorHandler.getDisplayMessage(error);
  this.showErrorMessage(userMessage);
  this.logError(error);
}
```

#### B. 키워드 추출 에러 처리
```javascript
// 현재: try-catch 없음
extractProductKeywords(productName) {
  const cleanedName = this.removeExcludedContent(productName);
  // 정규식 처리...
}

// 제안: 안전한 처리
extractProductKeywords(productName) {
  try {
    if (!productName) return [];
    const cleanedName = this.removeExcludedContent(productName);
    // 정규식 처리...
  } catch (error) {
    console.error('키워드 추출 실패:', error);
    return [];
  }
}
```

## 🚀 성능 모니터링 제안

### 1. **성능 측정 코드 추가**
```javascript
// 키워드 추출 성능 측정
extractProductKeywords(productName) {
  const startTime = performance.now();
  const result = this.doExtractKeywords(productName);
  const endTime = performance.now();
  
  if (endTime - startTime > 10) {
    console.warn(`키워드 추출 느림: ${endTime - startTime}ms`);
  }
  
  return result;
}
```

### 2. **메모리 사용량 모니터링**
```javascript
// 주기적 메모리 체크
setInterval(() => {
  if (performance.memory) {
    const used = performance.memory.usedJSHeapSize / 1024 / 1024;
    if (used > 100) { // 100MB 초과 시 경고
      console.warn(`메모리 사용량 높음: ${used.toFixed(2)}MB`);
    }
  }
}, 30000);
```

## 📝 코드 품질 개선

### 1. **함수 분리 (심각도: 중간)**

#### A. 너무 긴 함수들
```javascript
// tt.js의 highlightText() 함수 (140줄)
// 제안: 기능별로 분리
highlightText(text, compareWords, options) {
  if (options.isCorrectedCategory) {
    return this.highlightCorrectedCategory(text);
  }
  if (options.isCategoryDisplay) {
    return this.highlightCategoryDisplay(text);
  }
  return this.highlightRegularText(text, compareWords, options);
}
```

### 2. **매직 넘버 제거**
```javascript
// 현재: 하드코딩된 숫자들
if (now - this._tooltipTick < 120) return;
this.startCloseTimer(2000);

// 제안: 상수로 정의
const TOOLTIP_DEBOUNCE_TIME = 120;
const TOOLTIP_CLOSE_DELAY = 2000;
```

### 3. **타입 안정성 (선택사항)**
```javascript
// JSDoc 주석으로 타입 정보 추가
/**
 * 상품명에서 키워드를 추출합니다
 * @param {string} productName - 상품명
 * @returns {string[]} 추출된 키워드 배열
 */
extractProductKeywords(productName) {
  // ...
}
```

## 🎯 우선순위 정리

### 🔴 높은 우선순위 (즉시 수정 권장)
1. tt.js와 keyword.js 함수 중복 제거
2. 메모리 누수 방지 (이벤트 리스너 정리)
3. API 에러 처리 개선

### 🟡 중간 우선순위 (계획적 수정)
1. tt.js 파일 분리
2. 메모이제이션 적용
3. 이벤트 리스너 최적화

### 🟢 낮은 우선순위 (장기 계획)
1. 상태 관리 시스템 도입
2. 데이터 파일 최적화
3. 성능 모니터링 시스템

## 📊 예상 효과

### 성능 개선
- 초기 로딩 시간: 30% 감소 예상
- 메모리 사용량: 40% 감소 예상
- 키워드 추출 속도: 50% 향상 예상

### 유지보수성 개선
- 코드 중복: 80% 감소
- 버그 발생률: 60% 감소 예상
- 새 기능 개발 속도: 40% 향상 예상

### 코드 품질
- 함수 평균 길이: 50% 감소
- 순환 복잡도: 30% 감소
- 테스트 커버리지: 향후 80% 달성 가능