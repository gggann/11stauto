# Repository Guidelines

## 项目结构与模块组织
- 前端资源集中在仓库根目录，`index.html` 负责加载脚本与样式；`t.css` 与 `ttmodal.js` 定义 UI 与交互细节。
- 业务逻辑拆分在多个独立 JS 文件：例如 `Ann.svelte` 提供主要界面逻辑，`categoryRanking.js`、`keyword.js` 处理分类与关键词，`constants.js` 存放常量，`sub.js` 聚合复制、跳转等通用动作。
- 静态资产（如 `1.png`、`all.json`）与数据脚本混合存放，请在提交前确认新文件命名清晰且不与现有资源冲突。

## 构建、测试与开发命令
- 项目目前以静态资源形式交付，调试时可运行 `python -m http.server 8080` 或 `npx serve .` 在本地起一个 HTTP 服务，随后访问 `http://localhost:8080/index.html`。
- 针对接口连通性，可使用 `node test.js` 触发对内部 API (`http://10.240.129.13:3201/get-prd-info`) 的 POST 请求，验证返回字段与预期是否一致。
- 若需批量处理或压缩资源，请将脚本置于单独目录（例如 `tools/`），并在文档或提交说明中标注运行方式。

## 编码风格与命名规范
- JavaScript 统一使用 2 空格缩进、单引号与尾部逗号（仅多行对象/数组），Svelte 组件亦遵循相同缩进并优先使用明确的导入路径。
- 常量使用全大写蛇形命名（参考 `PRODUCT_STATUS`），模块导出函数采用驼峰命名，事件处理器前缀建议为 `handle`。
- 引入新的样式时请复用 `t.css` 中的变量或现有色板，避免在内联样式中写死颜色；共同依赖的工具函数放入 `sub.js` 或新增的 `utils/` 目录中。

## 测试指南
- 目前未配置自动化测试框架，推荐为关键算法或分类逻辑编写最小可复现脚本（例如在 `tests/` 目录中以 `*.spec.js` 命名）。
- 与远端依赖交互时，请为 `test.js` 添加环境开关（如读取 `.env` 中的 API 主机）并避免在默认分支提交真实账号信息。
- 在提交前至少执行一次 `node test.js` 或手工走通主业务流程，并在 PR 描述中总结测试结果与环境。

## 提交与 Pull Request 指南
- 建议采用 `type(scope): summary` 风格提交信息，例如 `feat(keyword): 支持多关键词高亮`；若包含脚本或资源更新，请在说明中补充影响面。
- PR 需附上变更背景、修改要点、测试结果与截图（适用于 UI 变更），并在标题中写明涉及模块；关联需求或缺陷请使用仓库约定的标签或链接。
- 审查前自检静态资源体积与加载顺序，确认未误删必要文件；如需引入新依赖，务必在说明中写明来源与用途。

## 安全与配置提示
- 内部 API 地址位于 `test.js`，上线前请确认是否需要切换到生产网段并遵循公司网络策略；若暴露在公网，请增加鉴权措施。
- 提交包含敏感数据的配置前请转为示例文件（如 `config.sample.json`），真实凭据使用环境变量或部署系统下发。
