# 코드 리팩토링 계획서

## 📋 분석 결과 요약

### 🔍 중복 선언된 함수들

#### 1. **extractKoreanSubstrings** 함수
- **위치**: `tt.js` (라인 1247, 1549)
- **문제**: 같은 기능이 두 번 구현됨
- **차이점**: 
  - 첫 번째: 복잡한 부분 문자열 추출 로직 (removeExcludedContent 포함)
  - 두 번째: 단순한 정규식 매칭
- **통합 방안**: 첫 번째 버전을 유지하고 두 번째 제거

#### 2. **findCategoriesByMultipleKeywords** 함수
- **위치**: `tt.js` (라인 603, 1580)
- **문제**: 같은 이름으로 두 번 정의됨
- **차이점**: 
  - 첫 번째: 복잡한 점수 기반 매칭 및 정렬 로직
  - 두 번째: 단순한 필터링 로직
- **통합 방안**: 첫 번째 버전을 유지하고 두 번째 제거

#### 3. **updateHighlights** vs **updateHighlightsIfSearchCategoryValid**
- **위치**: `tt.js` 여러 곳에서 혼용
- **문제**: 비슷한 기능의 메서드가 분산됨
- **통합 방안**: 하나의 메서드로 통합하고 조건부 로직 추가

### 🔧 통합 가능한 함수들

#### 1. **키워드 추출 관련 함수들**
```javascript
// 통합 대상
- extractKoreanSubstrings()
- extractProductKeywords()
- extractAllKeywordsFromCategories()
- getLeafKeywordsForAllCategories()
```
**통합 방안**: `KeywordExtractor` 클래스로 통합

#### 2. **카테고리 검색 관련 함수들**
```javascript
// 통합 대상
- findCategoriesByMultipleKeywords()
- findCategoriesByMultipleInputKeywords()
- categoryContainsKeyword()
```
**통합 방안**: `CategorySearcher` 클래스로 통합

#### 3. **하이라이트 관련 함수들**
```javascript
// 통합 대상
- highlightText()
- updateHighlights()
- clearHighlights()
- applyKeywordHighlightToCategory()
```
**통합 방안**: `HighlightManager` 클래스로 통합

#### 4. **툴팁 관련 함수들**
```javascript
// 이미 tooltip.js로 분리됨 (✅ 완료)
- calculateTooltipPosition()
- createTooltipStyle()
- generateTooltipHTML()
```

### 🎨 템플릿 분리 가능성

#### 1. **상품 카드 템플릿**
- **현재**: `tt.js` 내부에 거대한 인라인 템플릿 (라인 38-230)
- **분리 방안**: 
  ```
  templates/
  ├── ProductCard.vue
  ├── CategoryInput.vue
  ├── RecommendationList.vue
  └── ImageModal.vue
  ```

#### 2. **모달 템플릿**
- **현재**: `ttmodal.js`에 인라인 템플릿
- **분리 방안**: 
  ```
  templates/
  ├── LoginModal.vue
  └── SettingsModal.vue
  ```

#### 3. **설정 패널 템플릿**
- **현재**: `index.html` 내부에 복잡한 설정 UI
- **분리 방안**: `SettingsPanel.vue`

## 🚀 리팩토링 실행 계획

### Phase 1: 중복 함수 제거 (우선순위: 높음)
1. `extractKoreanSubstrings` 중복 제거
2. `findCategoriesByMultipleKeywords` 중복 제거
3. `updateHighlights` 관련 메서드 통합

### Phase 2: 유틸리티 클래스 생성 (우선순위: 중간)
1. `KeywordExtractor` 클래스 생성
2. `CategorySearcher` 클래스 생성
3. `HighlightManager` 클래스 생성

### Phase 3: 템플릿 분리 (우선순위: 낮음)
1. 상품 카드 컴포넌트 분리
2. 모달 컴포넌트 분리
3. 설정 패널 컴포넌트 분리

### Phase 4: 파일 구조 개선
```
src/
├── components/
│   ├── ProductCard/
│   │   ├── ProductCard.vue
│   │   ├── CategoryInput.vue
│   │   └── RecommendationList.vue
│   ├── Modals/
│   │   ├── LoginModal.vue
│   │   └── SettingsModal.vue
│   └── Settings/
│       └── SettingsPanel.vue
├── utils/
│   ├── KeywordExtractor.js
│   ├── CategorySearcher.js
│   ├── HighlightManager.js
│   └── TooltipManager.js
├── services/
│   └── ApiService.js
└── constants/
    ├── ProductStatus.js
    └── CategoryValidation.js
```

## 📊 예상 효과

### 코드 품질 개선
- **중복 코드 제거**: ~200줄 감소
- **가독성 향상**: 함수별 책임 명확화
- **유지보수성 향상**: 모듈화된 구조

### 성능 개선
- **번들 크기 감소**: 중복 코드 제거로 ~15% 감소
- **메모리 사용량 감소**: 중복 함수 인스턴스 제거

### 개발 효율성 향상
- **재사용성 증가**: 유틸리티 클래스 활용
- **테스트 용이성**: 단위별 테스트 가능
- **확장성 향상**: 새 기능 추가 시 기존 구조 활용

## ⚠️ 주의사항

1. **기존 기능 보존**: 리팩토링 중 기능 변경 금지
2. **점진적 적용**: Phase별 순차 진행
3. **테스트 필수**: 각 Phase 완료 후 기능 테스트
4. **백업 필수**: 리팩토링 전 현재 코드 백업

## 🎯 권장 우선순위

1. **즉시 실행**: 중복 함수 제거 (Phase 1)
2. **단기 목표**: 유틸리티 클래스 생성 (Phase 2)
3. **장기 목표**: 템플릿 분리 및 구조 개선 (Phase 3-4)