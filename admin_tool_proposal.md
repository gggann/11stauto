# [기획서] 이커머스 상품 카테고리 관리 어드민 툴 (Admin Tool)

## 1. 개요 (Overview)
본 문서는 대량의 상품 데이터를 효율적으로 관리하고, 상품명과 카테고리 간의 정합성을 검증/수정하기 위한 **Web 기반 어드민 툴** 구축 제안서입니다.

### 1.1 기존 시스템 문제점 (As-Is)

현재 사용 중인 **검색관리도구**의 카테고리 검색 기능에는 다음과 같은 심각한 문제가 있습니다.

| 문제 | 상세 설명 | 영향 |
| :--- | :--- | :--- |
| **서버 왕복 방식 검색** | 키워드 입력 시마다 서버로 요청 전송 → 서버에서 검색 → 결과 반환 | 매번 **0.5~2초 지연** 발생, 작업 흐름 끊김 |
| **불완전한 검색 커버리지** | 일부 카테고리가 키워드 검색에서 **누락**됨 (검색 불가) | 원하는 카테고리를 **찾을 수 없음**, 수동 탐색 필요 |
| **낮은 작업 효율성** | 느린 응답 + 누락 카테고리 → 반복적인 재검색 필요 | 동일 작업량 처리에 **2~3배 시간 소요** |

### 1.2 개선 방안 (To-Be)

본 어드민 툴에서는 위 문제를 해결하기 위해 다음과 같이 구현합니다.

| 개선 항목 | 구현 방식 | 효과 |
| :--- | :--- | :--- |
| **클라이언트 사전 로딩** | 페이지 로드 시 **모든 유효 카테고리 목록**을 미리 다운로드하여 브라우저 메모리에 저장 | 검색 시 **서버 통신 불필요** |
| **브라우저 내 즉시 필터링** | 사용자가 글자를 입력할 때마다 **JavaScript로 즉시 필터링** | **0ms 응답** (지연 없음) |
| **완전한 카테고리 커버리지** | 전체 카테고리 목록을 사전 로딩하므로 **모든 카테고리 검색 가능** | 누락 문제 **완전 해결** |
| **Server Round-Trip 절대 금지** | 자동완성 검색 시 서버 요청 **절대 발생하지 않음** | 네트워크 지연 **원천 차단** |

> **결론**: 기존 시스템의 서버 왕복 방식을 **클라이언트 사이드 사전 로딩 + 즉시 필터링**으로 완전 대체하여, 작업 효율성을 **3배 이상 향상**시킵니다.

---

## 2. 사용자 경험 및 디자인 (UI/UX)

### 2.1 메인 데이터 그리드 (Data Grid)
상품 목록을 테이블 형태로 표시하며, 각 행(Row)은 하나의 상품을 나타냅니다.

| 구성 요소 | 설명 |
| :--- | :--- |
| **체크박스** | 다중 선택을 위한 Checkbox. 여러 상품을 선택하여 일괄 작업 가능. |
| **상품 이미지** | 상품의 대표 이미지를 축소된 썸네일로 표시. |
| **상품명** | 상품의 이름. **키워드 하이라이트**가 적용되어 중요 키워드가 색상으로 강조됨. |
| **검색 카테고리** | 현재 상품에 매핑된 검색 카테고리 경로 (예: "패션의류 > 여성의류 > 원피스"). |
| **수정 카테고리** | 작업자가 새로운 카테고리를 입력하는 필드. 자동완성 지원. |
| **외부 링크** | 네이버 쇼핑 검색 버튼(N 아이콘). 클릭 시 해당 상품을 네이버에서 검색. |

---

## 3. 핵심 기능 명세 (Functional Requirements)

### 3.1 고급 필터링 (Advanced Filtering)

작업자가 원하는 상품만 빠르게 찾을 수 있도록 **검색 필터**를 제공합니다.

#### 기능 설명

| 기능 | 사용법 | 동작 |
| :--- | :--- | :--- |
| **포함 검색 (AND)** | 쉼표(`,`)로 키워드 구분 | 입력한 **모든 키워드**가 포함된 상품만 표시 |
| **제외 검색** | 키워드 앞에 마이너스(`-`) | 해당 키워드가 포함된 상품은 **숨김** |

#### 예시

> **예시 1: 포함 검색**
> - 입력: `운동화, 나이키`
> - 결과: 상품명에 '운동화' **그리고** '나이키'가 **모두** 있는 상품만 표시됨.

> **예시 2: 제외 검색**
> - 입력: `운동화, -아동`
> - 결과: '운동화'는 포함하되, '아동'이라는 단어가 **없는** 상품만 표시됨.

---

### 3.2 수정 카테고리 입력 필드 (마우스 오버 표시)

화면의 복잡도를 줄이고 작업 집중도를 높이기 위해, **수정 카테고리 입력 필드**는 특별한 방식으로 표시됩니다.

#### 상세 동작 설명

| 상태 | 화면 표시 | 설명 |
| :--- | :--- | :--- |
| **평소 (기본 상태)** | 입력창 **숨김** 또는 **얇은 선**만 표시 | 화면에 100개 상품이 나열될 때 입력창이 모두 보이면 복잡해 보임. 따라서 평소에는 숨겨둠. |
| **마우스를 올렸을 때 (Hover)** | 입력창이 **펼쳐짐** (display: block) | 작업자가 특정 상품 행(Row)에 마우스를 가져가면, 그 행의 수정 카테고리 입력창만 활성화되어 나타남. |
| **마우스를 벗어났을 때** | 입력창 다시 **숨김** | 다른 곳으로 마우스가 이동하면 이전 입력창은 다시 숨겨짐. |

#### 왜 이렇게 하는가?

1.  **시각적 피로도 감소**: 100개 상품이 있을 때 100개의 입력창이 전부 보이면 눈이 피로함.
2.  **집중도 향상**: 현재 작업 중인 상품에만 입력창이 보이므로 실수를 줄일 수 있음.
3.  **화면 공간 절약**: 불필요한 UI 요소를 숨겨 더 많은 정보를 한 화면에 표시 가능.

---

### 3.2.1 ⚠️ [중요] 자동완성 검색은 반드시 클라이언트 사이드로 구현

> **[CRITICAL REQUIREMENT]**
> 
> 수정 카테고리 입력 시 제공되는 **자동완성(Autocomplete) 기능**은 반드시 아래 방식으로 구현해야 합니다.

#### ✅ 올바른 구현 방식 (Client-Side Pre-Loading)

| 항목 | 설명 |
| :--- | :--- |
| **데이터 로딩 시점** | 페이지 로드 시 **모든 유효 카테고리 목록을 미리 다운로드**하여 클라이언트(브라우저) 메모리에 저장 |
| **검색 방식** | 사용자가 입력할 때마다 **브라우저 내에서 즉시 필터링** (JavaScript) |
| **네트워크 요청** | 검색어 입력 시 **서버 통신 없음** (실시간 반응) |
| **응답 속도** | **즉각적** (0ms 지연) |

#### ❌ 잘못된 구현 방식 (Server Round-Trip) - 절대 금지

| 항목 | 설명 |
| :--- | :--- |
| **방식** | 사용자가 글자를 입력할 때마다 검색어를 서버로 전송 → 서버에서 검색 → 결과 반환 |
| **문제점** | 네트워크 지연(Latency)으로 인해 **입력할 때마다 0.5~2초 대기** 발생 |
| **결과** | 작업 속도 급격히 저하, 사용자 경험 최악, **작업자 불만 폭증** |

#### 구현 예시 (JavaScript)

```javascript
// ✅ 올바른 방식: 페이지 로드 시 카테고리 데이터 미리 로드
const allCategories = await fetch('/api/categories').then(r => r.json());
// allCategories = ['패션의류 > 여성의류 > 원피스', '패션의류 > 남성의류 > 티셔츠', ...]

// 사용자 입력 시 클라이언트에서 즉시 필터링
function searchCategories(query) {
  return allCategories.filter(cat => cat.includes(query)); // 서버 요청 없음!
}

// ❌ 잘못된 방식: 입력할 때마다 서버 요청
async function searchCategoriesBad(query) {
  return await fetch(`/api/search?q=${query}`).then(r => r.json()); // 매번 서버 왕복!
}
```

> **⚠️ 경고**: 서버 왕복 방식으로 구현할 경우, 작업자가 100개 상품을 처리하는 동안 수백 번의 불필요한 서버 요청이 발생하며, 이는 서버 부하 증가 + 사용자 경험 저하로 이어집니다.

---

### 3.3 키워드 하이라이트 (Keyword Highlight)

상품명의 키워드와 카테고리 간의 **일치 여부**를 색상으로 시각화합니다.

#### 색상 규칙

| 색상 | 코드 | 조건 | 의미 |
| :---: | :---: | :--- | :--- |
| **파랑색** | #1976d2 | 상품명의 키워드가 검색 카테고리의 **상위 depth(1~중간)**에 존재 | 대분류/중분류 키워드 일치 (양호) |
| **빨간색** | #dc3545 | 상품명의 키워드가 검색 카테고리의 **리프(Leaf, 최하위 depth)**에 존재 | 세부 카테고리 키워드 일치 (주의 깊게 확인) |

#### 상세 로직 (Step-by-Step)

```
1단계: 상품명에서 2글자 이상의 한글 키워드를 모두 추출합니다.
       예) "나이키 남성 운동화" → ['나이키', '남성', '운동화']

2단계: 검색 카테고리 경로를 depth별로 분리합니다.
       예) "패션의류 > 남성신발 > 운동화"
           → 1-depth: "패션의류"
           → 2-depth: "남성신발"
           → 3-depth(리프): "운동화"

3단계: 추출된 각 키워드를 카테고리 depth와 비교합니다.

4단계: 비교 결과에 따라 색상 적용
       - 키워드가 1~중간 depth에 있으면 → 파랑색
       - 키워드가 리프(최하위) depth에 있으면 → 빨간색
       - 어디에도 없으면 → 하이라이트 없음

5단계: 성별/연령 키워드('남성', '여성', '아동' 등)는 
       추가로 Bold + 테두리(Border)로 강조합니다.
```

#### 예시

> **예시 1**
> - 상품명: `"나이키 남성 운동화 (흰색)"`
> - 검색 카테고리: `"패션의류 > 남성신발 > 운동화"`
> - 결과:
>   - '패션의류' → 상품명에 없음 → 하이라이트 없음
>   - '남성신발' → 상품명에 없음 → 하이라이트 없음
>   - '남성' → 상품명에 있음 + 카테고리 중간에 있음 → <span style="color:#1976d2">**파랑색**</span> + Bold
>   - '운동화' → 상품명에 있음 + 카테고리 **리프**에 있음 → <span style="color:#dc3545">**빨간색**</span>

---

### 3.4 키워드 클릭 상호작용 (Click-to-Fill)

하이라이트된 키워드를 **클릭**하면 수정 카테고리 입력란에 자동으로 입력됩니다.

#### 동작 방식

| 동작 | 설명 |
| :--- | :--- |
| **첫 번째 클릭** | 클릭한 키워드가 '수정 카테고리' 입력란에 자동 입력됨. |
| **같은 키워드 재클릭** | 이미 입력된 키워드와 같으면 입력란이 **비워짐** (토글 동작). |
| **다른 키워드 클릭** | 기존 입력 내용이 **새 키워드로 교체**됨. |

#### 예외 처리 (오입력 방지)

- '남성', '여성', '아동' 등 **성별/연령 키워드**는 클릭해도 바로 입력되지 않음.
- 이유: 이러한 키워드만으로는 정확한 카테고리를 특정할 수 없어 오입력 가능성이 높음.

---

### 3.5 성별/연령대 정합성 검사 (Gender & Age Validation)

상품명과 카테고리 간의 **성별/연령 일치 여부**를 자동으로 검사합니다.

#### 키워드 그룹 정의

| 그룹 | 포함 키워드 |
| :--- | :--- |
| 성인 남성 | 남성, 남자 |
| 성인 여성 | 여성, 여자, 우먼 |
| 성인 공용 | 남녀, 남여, 유니섹스 |
| 아동(키즈) | 키즈, 여아, 주니어, 남아, 아동, 어린이, 유아 |

#### 검증 로직

| 상황 | 결과 |
| :--- | :--- |
| 상품명: '남성' / 카테고리: '여성' | ⚠️ **경고** - 성별 불일치 |
| 상품명: '여성' / 카테고리: '아동' | ⚠️ **경고** - 연령 불일치 (성인→아동) |
| 상품명: '키즈' / 카테고리: '남성' | ⚠️ **경고** - 연령 불일치 (아동→성인) |
| 상품명: '남녀공용' / 카테고리: 아무거나 | ✅ **허용** - 공용이므로 무관 |

---

### 3.6 대량 작업 및 일괄 업데이트 (Bulk Operations)

#### 일괄 업데이트 버튼

작업자가 여러 상품을 검수/수정한 후, **'저장' 버튼 한 번**으로 모든 변경 사항을 서버에 전송합니다.

| 동작 | 설명 |
| :--- | :--- |
| **저장 버튼 클릭** | 현재 화면의 모든 수정 내역이 서버로 전송됨. |
| **결과** | 수정된 상품은 '처리 완료'로 표시되고, 새로운 100개 상품이 자동 로드됨. |

---

### 3.7 작업 배분 및 페이징 (Work Allocation & Pagination)

#### 모드 1: 자동 배분 시스템 (Auto-Allocation Cycle)

```
[작업자 접속]
     ↓
[서버에서 미처리 상품 100개 자동 할당]
  ※ 같은 카테고리끼리 묶어서 정렬하여 할당 (모니터링 효율 ↑)
     ↓
[작업 수행]
  - 정상 상품 → 수정 카테고리 란을 비워둠
  - 오류 상품 → 수정 카테고리 란에 올바른 값 입력
     ↓
[일괄 업데이트 버튼 클릭]
     ↓
[서버로 결과 전송]
     ↓
[새로운 미처리 상품 100개 자동 할당] ← 반복
```

> **카테고리 그룹핑**: 자동 할당 시 비슷한 카테고리의 상품을 연속으로 배치하여, 작업자가 유사한 상품을 연속으로 처리할 수 있도록 함.

#### 모드 2: 수동 상품번호 입력 (Manual Bulk Input)

자동 배분 외에도, 기존 방식처럼 **상품번호를 대량으로 직접 입력**하여 카드 뷰를 생성하는 기능도 제공합니다.

| 기능 | 설명 |
| :--- | :--- |
| **상품번호 대량 입력** | 텍스트 영역에 상품번호를 줄바꿈 또는 공백으로 구분하여 붙여넣기 |
| **카드 뷰 생성** | 입력된 상품번호에 해당하는 상품들이 카드 형태로 화면에 표시됨 |
| **용도** | 특정 상품 목록을 지정하여 작업해야 할 때, QA 검수 시 특정 상품 확인 등 |

> **두 가지 모드 병행**: 일반 작업은 자동 배분으로 효율적으로 처리하고, 특수한 경우(특정 상품 검수, 재작업 등)에는 수동 입력 모드를 사용.

#### 페이징 처리

- 한 화면에 **100개** 상품만 표시하여 스크롤을 최소화하고 집중도를 높임.

---

### 3.8 통계 및 이력 관리 (Statistics & History)

#### 작업자별 통계 (Worker Stats)

| 항목 | 설명 |
| :--- | :--- |
| **모니터링 (Total)** | 작업자가 할당받아 검토한 전체 상품 수 |
| **수정 처리 (Modified)** | 실제로 카테고리를 수정/입력한 상품 수 |
| **수정률 (Rate)** | `(수정 처리 / 모니터링) × 100` (품질 지표) |

#### 통계 대시보드 예시

> **[가상 화면] 작업자별 성과 분석 (2026-01-01 ~ 2026-01-31)**

| 날짜 | 작업자명 | 모니터링 | 수정 처리 | 수정률 | 비고 |
| :---: | :---: | :---: | :---: | :---: | :--- |
| 2026-01-29 | 홍길동 | 500 | 125 | 25.0% | (정상) |
| 2026-01-29 | 김철수 | 480 | 48 | 10.0% | ⚠️ 수정률 저조 |
| 2026-01-28 | 이영희 | 520 | 312 | 60.0% | ⚠️ 과다 수정 의심 |
| **합계** | **3명** | **1,500** | **485** | **32.3%** | - |

#### 처리 이력 조회 (Audit Log)

- **목적**: 품질 검수(QA) 및 오처리 파악
- **검색 조건**: 처리 일자(Date), 작업자 이름(Name)
- **용도**: 처리된 내역을 역추적하여 품질 관리

---

## 4. 기술 스택 및 구현 방식 (Technical Implementation)

### 4.1 하이라이트 로직 구현

```javascript
// 1. 상품명에서 키워드 추출
const keywords = extractKoreanSubstrings(productName); // ['나이키', '남성', '운동화']

// 2. 카테고리 리프(최하위) 추출
const leafCategory = categoryPath.split(' > ').pop(); // '운동화'

// 3. 각 키워드에 대해 색상 결정
keywords.forEach(keyword => {
  if (leafCategory.includes(keyword)) {
    // 리프에 있으면 → 빨간색
    applyHighlight(keyword, '#dc3545');
  } else if (categoryPath.includes(keyword)) {
    // 상위 depth에 있으면 → 파랑색
    applyHighlight(keyword, '#1976d2');
  }
});
```

### 4.2 검색 필터 파서

```javascript
// 입력: "운동화, -아동"
// 결과:
//   includeKeywords = ['운동화']
//   excludeKeywords = ['아동']
```

---

*※ 본 문서는 개발 요청서입니다. 실제 구현 시 상세 사항은 협의가 필요합니다.*
