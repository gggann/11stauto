# 修订记录

## 2025-10-21
- 精简 `tt.js` 内重复的关键词解析/分类函数，统一复用 `keyword.js` 提供的工具。
- 重写推荐提示框为模板驱动实现，移除手动挂载、`innerHTML` 更新及 `removeAllTooltips` 依赖。
- 新增 `uiFlags` 与 `shouldShowCategoryRecommendation` 计算属性，模板和逻辑统一通过计算属性获取展示开关，减少 `$parent` 直接耦合。
- 优化关键词高亮事件绑定：集中在 `attachKeywordEvents`/`detachKeywordEvents`，在组件销毁前清理监听避免内存泄漏。
- 新增 `categorySearchInput` 与相关方法，保持搜索框状态与筛选条件同步，并利用响应式渲染刷新推荐列表。
- 统一通过 `KeywordProcessor.*` 调用关键词工具，避免静态方法脱离类上下文造成的 `removeExcludedContent` 未定义错误。
- 调整 tooltip 渲染策略：改为重新渲染并挂载到 `body`，同时复用 `generateTooltipHTML` 与事件绑定，恢复与旧版一致的定位与交互体验。
7. **视觉优化**
   - 重构 tooltip HTML/CSS，引入卡片化样式、搜索栏与动作按钮，提升可读性。
   - 多关键词推荐改为圆角 chip 风格，支持悬浮动画与响应式布局。
8. **Tooltip 精简**
   - 减小 tooltip 外框尺寸，移除边框并下调阴影，改用紧凑布局。
   - 头部、列表与 chip 间距收紧，呈现更紧凑的信息密度。
9. **Tooltip 进一步紧凑化**
   - 去掉 category chip 边框与箭头，缩小上下内边距和字号。
   - Header 改为 8px padding，标题字号降至 13px，实现更紧凑布局。
10. **Tooltip 列表无边框**
    - category-tooltip__list 去除背景/边框并收紧 padding，使内容区域更加贴合。
11. **Tooltip 列表项重新设计**
    - category-chip 样式及引用全部移除，改用 `.category-tooltip__item` with gradient 背景与小圆点指示。
