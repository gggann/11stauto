# 11st Gemini Tool 개발 히스토리

> 이 문서는 11번가 상품 카테고리 매칭 툴의 주요 기능 개발 내역과 변경 사항을 기록합니다.

---

## 🏗️ 1. 주요 기능 (Major Features)

### 🖥️ 뷰 모드 제어 시스템 (View Mode Control)
작업 효율성을 극대화하기 위해 단축키로 UI 레이아웃을 즉시 전환하는 기능입니다.
- **주요 로직**: `t.js` (Key Event Listening), `tt.js` (Conditional Rendering)
- **모드 구성**:
    - **F6 (Image Only)**: 텍스트 없이 이미지만 빠르게 훑어보기
    - **F7 (Image + Search Category)**: 이미지와 현재 매칭된 검색 카테고리 비교
    - **F8 (Image + Search + Corrected)**: 수정 작업에 최적화된 뷰
    - **F9 (Name + Corrected)**: 상품명 텍스트 기반 정밀 검수

### 📊 데이터 로딩 및 처리 (Data Handling)
대량의 상품 데이터를 효율적으로 불러오고 관리하는 엔진입니다.
- **초기 구현**: `t.js` 내 API 직접 호출 방식
- **개선 사항**: Excel 파일(.xlsx) 드래그 앤 드롭 로딩 지원 (대량 데이터 처리 속도 개선)
- **데이터 구조**: 상품번호(`id`), 상품명(`dealname`), 카테고리 정보 등을 객체 배열로 관리

### ✅ 카테고리 검증 자동화 (Category Validation)
휴먼 에러를 방지하기 위한 실시간 검증 시스템입니다.
- **유효성 검사**: `검카체크.js`의 화이트리스트 기반 카테고리 명칭 검증
- **오타 교정**: 입력 시 실시간으로 유효하지 않은 카테고리 감지 및 경고
- **성별/연령 판별**: `성별판별.js`를 통해 상품명과 카테고리의 성별 불일치(남성 팬티 ↔ 여성 카테고리 등) 자동 감지

### ⌨️ 입력 편의 및 UX (user Experience)
반복 작업을 줄이기 위한 유틸리티 기능들입니다.
- **스마트 클립보드 (`sub.js`)**:
    - HTTP/HTTPS 환경 자동 감지 및 Fallback 처리
    - 수정된 카테고리 목록 엑셀 붙여넣기용 포맷 자동 복사
- **이미지 돋보기 (`bigimage.js`)**: 마우스 오버/클릭 시 고해상도 이미지 모달 출력
- **다중 선택 (Multi-select)**: 체크박스 및 키워드 필터링을 통한 일괄 카테고리 적용

---

## 📅 2. 개발 일지 (Changelog)

### v2025.12.16
- **[Feature] 뷰 모드(F7~F9) 재정의**: 사용자 요청에 따라 단축키별 표시 정보 변경
    - **F7**: 상품명 + 검색카테고리 + 수정카테고리
    - **F8**: 검색카테고리 + 수정카테고리
    - **F9**: 상품명 + 수정카테고리
- **[Revert] 뷰 옵션 통합 롤백**: 뷰 컨트롤 패널 제거 및 단축키 기반 모드로 복귀
- **[UI] 카드 간격 조정**: 카드 간 여백을 축소하여(8px -> 2px) 더 많은 상품을 한 화면에 표시
- **[UI] 상세 정보 레이아웃 변경**:
    - 카탈로그 번호 숨김 (`display: none`)
    - 상품유형(단일상품여부)을 셀러 정보와 같은 행에 표시
    - 브랜드 정보를 셀러 정보와 같은 행에 표시 (셀러 | 카탈로그 | 브랜드 | 상품유형)
- **[UI] 상품 이미지 순번 표시**: 이미지 좌측 상단에 1부터 시작하는 순서 번호 배지 추가
    - 스타일: 검정색 텍스트, 작은 폰트, 배경 없음 (무장식 심플 스타일)
    - 스타일: 검정색 텍스트, 작은 폰트, 배경 없음 (무장식 심플 스타일)
    - 글자색: **회색(#888)**으로 변경
- ~~**[Feature] 입력 제한 기능 (Cancelled)**~~: 사용자 요청으로 상품번호 300개 제한 기능 제거

### v2025.12.15
- **[Fix] HTTP 클립보드 이슈 해결**: 보안 컨텍스트(HTTPS)가 아닌 환경에서도 복사가 작동하도록 `execCommand` fallback 로직 구현 (`sub.js`)
- **[Refactor] 뷰 모드 체계 개편**: 작업자 피드백을 반영하여 F6~F9 단축키의 동작 방식을 실무 중심으로 재정의
    - F7: 전시카테고리 제거 → 검색카테고리 표시로 변경
    - F9: 이미지 제거 → 텍스트 집중 모드로 변경

### v2025.12.13
- **[UI] 컴팩트 뷰 최적화**: 이미지 전용 모드 등에서 불필요한 공백을 제거하여 화면 활용도 극대화
- **[Feature] 카드 높이 자동 조절**: 내용물에 따라 카드 크기가 유동적으로 변하도록 CSS/JS 로직 개선

### v2025.12.12
- **[Feature] 엑셀 업로드 도입**: 기존 API 로딩 방식의 속도 저하 문제를 해결하기 위해 로컬 엑셀 파일 파싱 로직 추가
- **[Fix] 설정 모달 버그 수정**: 설정 버튼(⚙️) 클릭 시 모달이 열리지 않던 문제 해결

---

## 🚀 3. 다음 목표 (Roadmap)
- [ ] **자동 저장 기능**: 브라우저 종료 시 작업 내역 로컬 스토리지 저장 강화
- [ ] **키워드 하이라이트 고도화**: 사용자 지정 키워드 색상 변경 기능
- [ ] **통계 대시보드**: 작업 진행률 및 카테고리별 분포 그래프 시각화
